pragma solidity 0.6.11;

enum Enum1 { A, B, C }

enum Enum2 { E, F, G }

enum Struct1_1 { H, I, J }

struct Struct1 {
    uint256 x;
}

contract Imp1 {}

/// Utility contract holding a stack counter
contract __scribble_ReentrancyUtils {
    bool __scribble_out_of_contract = true;
}

contract Foo is Imp1 {
    Struct1 internal s;
    Enum1 internal e1;
    Enum2 internal fe;
    Struct1_1 internal e3;
}
